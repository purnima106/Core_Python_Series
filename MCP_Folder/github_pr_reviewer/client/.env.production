# Production / Docker environment
# Used automatically by Vite when running: npm run build
#
# "backend" is the Docker Compose service name — resolvable inside the Docker network.
# The browser NEVER calls this directly; Nginx proxies /api/* to the backend container.
# See the nginx.conf proxy_pass rule for /api/.
VITE_API_URL=http://backend:8000
